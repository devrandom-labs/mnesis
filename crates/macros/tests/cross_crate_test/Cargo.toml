[package]
name = "mnesis-cross-crate-test"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
mnesis = { path = "../../../mnesis", features = ["derive"] }
thiserror = "2"
workspace-hack = { version = "0.1", path = "../../../workspace-hack" }
