---
title: CQRS
description: Separating the write model (aggregates deciding events) from read models (projections over the log).
---

# CQRS

<Info>This page is coming soon.</Info>

Separating the write model (aggregates deciding events) from read models (projections over the log).

**In the meantime:** the write side is [Model a Domain](/model-a-domain/aggregates); read models are built from projection primitives — see the [`projection-tokio` example](https://github.com/devrandom-labs/mnesis/tree/main/examples/projection-tokio).
