---
title: Hexagonal Architecture
description: The pure kernel at the centre, storage adapters at the edge — mnesis' crate boundary is the port/adapter seam.
---

# Hexagonal Architecture

<Info>This page is coming soon.</Info>

The pure kernel at the centre, storage adapters at the edge — mnesis' crate boundary is the port/adapter seam.

**In the meantime:** the seam is the `RawEventStore` + `WakeSource` traits — see [Writing a Store Adapter](/go-live/writing-a-store-adapter).
