---
title: Handle & Decide
description: The Handle<C> trait: a pure decision function of (state, command) that returns decided events.
---

# Handle & Decide

<Info>This page is coming soon.</Info>

The Handle<C> trait: a pure decision function of (state, command) that returns decided events.

**In the meantime:** read the [`mnesis` API docs](https://docs.rs/mnesis); the [Quickstart](/getting-started) shows several handlers.
